export enum StatisticsType {
    new_stat_items = 'new_stat_items',
    updated_stat_items = 'updated_stat_items',
    rejected_stat_items = 'rejected_stat_items',
    error_stat_items = 'error_stat_items'
}